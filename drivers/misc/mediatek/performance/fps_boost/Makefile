# fps_boost - frame-aware CPU boost (experimental)
# Consumes ged_kpi_fps_notify_fp (see ged_kpi.c) for per-main-head frame cadence.
obj-y += fps_boost.o

ccflags-y += -I$(srctree)/drivers/misc/mediatek/gpu/ged/include
